function WordCount(str) {
    var totalSoFar = 0;
    for (var i = 0; i < WordCount.length; i++)
      if (str(i) === " ") { 
        totalSoFar = +1;
    }
    totalsoFar += 1;
  }
  
  console.log(WordCount("Random String"));